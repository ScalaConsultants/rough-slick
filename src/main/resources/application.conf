db {
  driver = "org.postgresql.Driver"
  jdbc.url = "jdbc:postgresql://127.0.0.1/slick_tryouts"
  user = "root"
  password = ""
}

join-perf-test {
  n-suppliers = 10
  n-products = 50
  n-purchasers = 20
  n-sales = 100

  select-threshold = 0.9
  n-queries = 100
}